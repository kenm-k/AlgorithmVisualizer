var rObjX;

function tilford_raingold()
{
    var root = document.getElementById("treeRoot").value;
    rObjX = new Array(count);


}

function tilford_raingold_sub(v)
{
    var childs = graph["graphs"][frame][graph][v];
    if (childs.length == 0) return 0;
}